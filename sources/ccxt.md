# CCXT

Library: `ccxt` (npm). Unified spot REST: `loadMarkets`, `fetchBalance`, `fetchTicker`, `createOrder`, `fetchOrder`, `cancelOrder`, `editOrder` when present.

Docs: https://docs.ccxt.com

Venue ids in this app map to CCXT ids in `src/server/venues.ts`.
